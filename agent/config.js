/* Public configuration only. Never put model API keys or private documents here.
 * Set endpoint to the HTTPS backend described in README.md after it is deployed.
 */
window.YEUHUB_AGENT_CONFIG = Object.freeze({
  endpoint: '',
  knowledgeReady: false,
  knowledgeDescription: ''
});
