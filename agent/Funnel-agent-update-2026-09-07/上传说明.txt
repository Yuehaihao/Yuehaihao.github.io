增长漏斗图更新包

仅上传以下内容以覆盖漏斗图工具：
1. 将 agent 文件夹整体上传到 GitHub 仓库根目录并覆盖。
2. 如仓库已有 assets/access.js，可保留原文件；若缺失，再上传本包 assets/access.js。
3. 不需要覆盖首页 index.html、fitness 或其他文件夹。

本次更新：
- 数字直接显示并可编辑于漏斗图内。
- 数字后支持编辑变化值，例如 100（+5）。
- 百分比按“当前层数据 ÷ 顶层数据”自动计算。
- 百分比后支持编辑变化值，例如 97%（+5pp）。
